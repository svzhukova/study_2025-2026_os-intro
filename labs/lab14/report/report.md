---
## Front matter
title: "Лабораторная работа № 14"
subtitle: "Партиции, файловые системы, монтирование"
author: "Жукова София Викторовна"

## Generic otions
lang: ru-RU
toc-title: "Содержание"

## Bibliography
bibliography: bib/cite.bib
csl: pandoc/csl/gost-r-7-0-5-2008-numeric.csl

## Pdf output format
toc: true # Table of contents
toc-depth: 2
lof: true # List of figures
lot: true # List of tables
fontsize: 12pt
linestretch: 1.5
papersize: a4
documentclass: scrreprt
## I18n polyglossia
polyglossia-lang:
  name: russian
  options:
	- spelling=modern
	- babelshorthands=true
polyglossia-otherlangs:
  name: english
## I18n babel
babel-lang: russian
babel-otherlangs: english
## Fonts
mainfont: IBM Plex Serif
romanfont: IBM Plex Serif
sansfont: IBM Plex Sans
monofont: IBM Plex Mono
mathfont: STIX Two Math
mainfontoptions: Ligatures=Common,Ligatures=TeX,Scale=0.94
romanfontoptions: Ligatures=Common,Ligatures=TeX,Scale=0.94
sansfontoptions: Ligatures=Common,Ligatures=TeX,Scale=MatchLowercase,Scale=0.94
monofontoptions: Scale=MatchLowercase,Scale=0.94,FakeStretch=0.9
mathfontoptions:
## Biblatex
biblatex: true
biblio-style: "gost-numeric"
biblatexoptions:
  - parentracker=true
  - backend=biber
  - hyperref=auto
  - language=auto
  - autolang=other*
  - citestyle=gost-numeric
## Pandoc-crossref LaTeX customization
figureTitle: "Рис."
tableTitle: "Таблица"
listingTitle: "Листинг"
lofTitle: "Список иллюстраций"
lotTitle: "Список таблиц"
lolTitle: "Листинги"
## Misc options
indent: true
header-includes:
  - \usepackage{indentfirst}
  - \usepackage{float} # keep figures where there are in the text
  - \floatplacement{figure}{H} # keep figures where there are in the text
---

# Цель работы

Получить навыки создания разделов на диске и файловых систем. Получить навыки монтирования файловых систем.



# Выполнение лабораторной работы

**Создание виртуальных носителей**

Добавим к нашей виртуальной машине два диска размером 512 МБ. Добаляю на машину UTM (рис. [-@fig:001]).

![Создаем диск](image/1.png){#fig:001 width=70%}

Для добавления второго диска размером 512 МБ к контроллеру SATA повторим указанные выше действия.(рис. [-@fig:002]).

![2 создвнных диска](image/2.png){#fig:002 width=70%}

**Создание разделов MBR с помощью fdisk**


Запустим нашу виртуальную машину с добавленными дополнительными дисками
disk1 и disk2. В командной строке с полномочиями администратора с помощью fdisk посмотрим
перечень разделов на всех имеющихся в системе устройствах жёстких дисков (рис. [-@fig:003]).

![Посмотрим перечень разделов на всех жёстких дисках](image/3.png){#fig:003 width=70%}


Таблица разделов находится только в памяти ядра. Сравним вывод команды  (рис. [-@fig:004]).

![fdisk -l /dev/sdb](image/4.png){#fig:004 width=70%}

С выводом команды (рис. [-@fig:005]).

![cat /proc/partitions](image/5.png){#fig:005 width=70%}

**Создание логических разделов**

В терминале с полномочиями администратора запустим (рис. [-@fig:006]).

![Запустим](image/6.png){#fig:006 width=70%}

Введем e , чтобы создать расширенный раздел.
Теперь, когда расширенный раздел создан, мы можем создать в нём логический раздел.
Из интерфейса fdisk снова нажмите n . Утилита сообщит, что нет свободных первичных разделов и по умолчанию предложит добавить логический раздел с номером
Нажмем Enter , чтобы принять выбор первого сектора в качестве сектора по умолчанию.


После создания логического раздела введем w , чтобы записать изменения на диск
и выйти из fdisk. Чтобы завершить процедуру и обновить таблицу разделов, введем (рис. [-@fig:007]). 

![Введем](image/7.png){#fig:007 width=70%}

Просмотрим информацию о добавленных разделах:(рис. [-@fig:008]).

![Просмотрим](image/8.png){#fig:008 width=70%}

**Создание раздела подкачки**

Получим полномочия администратора. Запустим fdisk:
Нажмем n , чтобы добавить новый раздел. 
Нажмем Enter , чтобы принять первый сектор по умолчанию.
После создания логического раздела введем w , чтобы записать изменения на диск и выйти из fdisk. 
Новый раздел теперь готов к использованию.
Отформатируем раздел подкачки, используя команду (рис. [-@fig:009]).
 
![Отформатируем раздел подкачки](image/9.png){#fig:009 width=70%}

Для включения вновь выделенного пространства подкачки используем (рис. [-@fig:010]).

![Включение вновь выделенного пространства подкачки](image/10.png){#fig:010 width=70%}


Для просмотра размера пространства подкачки, которое в настоящее время выделено, введем (рис. [-@fig:011]).

![Введем](image/11.png){#fig:011 width=70%}

**Создание разделов GPT с помощью gdisk**

В терминале с полномочиями администратора с помощью gdisk посмотрим таблицы
разделов и разделы на втором добавленном вами ранее диске /dev/sdc (рис. [-@fig:012]).

![Посмотрим](image/12.png){#fig:012 width=70%}

Создадим раздел с помощью gdisk (рис. [-@fig:013]).

![Создадим](image/13.png){#fig:013 width=70%}

Программа gdisk попытается определить текущее разбиение диска, и если ничего не
обнаружено, то будет создана таблица разделов GPT и соответствующее разбиение
диска.
Введем n , чтобы добавить новый раздел. 
Теперь вас попросят задать первый сектор. По умолчанию будет использоваться пер-вый сектор, доступный на диске, но также можно указать смещение. Нажмем Enter ,чтобы принять предлагаемый по умолчанию первый сектор.
При запросе последнего сектора по умолчанию предлагается последний сектор, доступный на диске (создаётся раздел, который заполняет весь жёсткий диск). 
Теперь предлагается установить тип раздела. 
Теперь раздел создан (но ещё не записан на диск). Нажмем p , чтобы отобразить разбиение диска.
Если текущее разбиение устраивает, нажмите w , чтобы записать изменения на диск.
В третьей вкладке терминала создадим отдельный файл конфигурации для мониторинга отладочной информации 

Обновим таблицу разделов (рис. [-@fig:014]).

![Обновим](image/14.png){#fig:014 width=70%}

**Форматирование файловой системы XFS**

В терминале с полномочиями администратора для диска dev/sdb1 создадим файловую систему XFS  (рис. [-@fig:015]).


![Создадим файловую систему XFS](image/15.png){#fig:015 width=70%}

Для установки метки файловой системы в xfsdisk используем команду (рис. [-@fig:016]).

![Установка метки файловой системы](image/16.png){#fig:016 width=70%}

**Форматирование файловой системы EXT4** 

В терминале с полномочиями администратора для диска dev/sdb5 создадим файловую систему EXT4 (рис. [-@fig:017]).

![Создадим файловую систему EXT4](image/17.png){#fig:017 width=70%}

Для установки метки файловой системы в ext4disk используем команду (рис. [-@fig:018]).


![Используем](image/18.png){#fig:018 width=70%}

Для установки параметров монтирования по умолчанию для файловой системы используем команду (рис. [-@fig:019]).

![Параметры монтирования по умолчанию](image/19.png){#fig:019 width=70%}

**Ручное монтирование файловых систем**

Для ручной установки файловой системы используется команда mount. Чтобы отключить смонтированную файловую систему, используется команда umount.
Чтобы смонтировать файловую систему, используйте следующую команду
mount /dev/sdb5 /mnt/tmp (рис. [-@fig:020]).

![Чтобы отмонтировать раздел, можно использовать umount либо с именем устройства, либо с именем точки монтирования.](image/20.png){#fig:020 width=70%}


**Монтирование разделов с помощью /etc/fstab**

В этом упражнении подмонтируем отформатированный раздел XFS
/dev/sdb1, который был создан в предыдущих упражнениях.
Создадим точку монтирования для раздела XFS /dev/sdb1:
Посмотрим информацию об идентификаторах блочных устройств (UUID):
Введем blkid /dev/sdb1 и затем используйте мышь, чтобы скопировать значение
идентификатора UUID для устройства /dev/sdb1.
Откройте файл /etc/fstab на редактирование и добавьте следующую строку (рис. [-@fig:021]).

![В в реальном времени](image/21.jpeg){#fig:021 width=70%}

Перед попыткой автоматического монтирования при перезагрузке рекомендуется проверить конфигурацию. Проверим, что раздел примонтирован правильно 

**Самостоятельная работа**

Добавим две партиции на диск с разбиением GPT. Создадим оба раздела размером 100 MiB. Один из этих разделов должен быть настроен как пространство подкачки, другой раздел должен быть отформатирован файловой системой ext4.
Настроим сервер для автоматического монтирования этих разделов. Установите раздел ext4 на /mnt/data-ext и установим пространство подкачки в качестве области подкачки.  (рис. [-@fig:022]).

![Добавим две партиции на диск с разбиением GPT](image/22.jpeg){#fig:022 width=70%}



**Контрольные вопросы**


1. **Какой инструмент используется для создания разделов GUID?**
   - Инструмент для создания разделов GUID — это `gdisk`. Он предназначен для работы с GPT (GUID Partition Table).

2. **Какой инструмент применяется для создания разделов MBR?**
   - Для создания разделов в MBR (Master Boot Record) используется `fdisk` или `parted`. 

3. **Какой файл используется для автоматического монтирования разделов во время загрузки?**
   - Файл, используемый для автоматического монтирования разделов во время загрузки, — это `/etc/fstab`.

4. **Какой вариант монтирования целесообразно выбрать, если необходимо, чтобы файловая система не была автоматически примонтирована во время загрузки?**
   - В этом случае следует использовать вариант `noauto` в `/etc/fstab`. Этот параметр указывает системе не монтировать файловую систему автоматически.

5. **Какая команда позволяет форматировать раздел с типом 82 с соответствующей файловой системой?**
   - Для форматирования раздела с типом 82 (Linux swap) используется команда:
     ```bash
     mkswap /dev/sdXY
     ```
     где `/dev/sdXY` — это нужный раздел.

6. **Вы только что добавили несколько разделов для автоматического монтирования при загрузке. Как можно безопасно проверить, будет ли это работать без реальной перезагрузки?**
   - Для проверки можно использовать команду:
     ```bash
     mount -a
     ```
     Эта команда попытается смонтировать все файловые системы, указанные в `/etc/fstab`.

7. **Какая файловая система создаётся, если вы используете команду `mkfs` без какой-либо спецификации файловой системы?**
   - Если использовать `mkfs` без спецификации, по умолчанию создаётся файловая система ext2.

8. **Как форматировать раздел EXT4?**
   - Для форматирования раздела в файловую систему EXT4 используется команда:
     ```bash
     mkfs.ext4 /dev/sdXY
     ```
     где `/dev/sdXY` — это нужный раздел.

9. **Как найти UUID для всех устройств на компьютере?**
   - Чтобы найти UUID для всех устройств, можно использовать команду:
     ```bash
     blkid
     ```

# Выводы

Мы получили навыки создания разделов на диске и файловых систем и навыки монтирования файловых систем.
